---
title: "Estadística Multidimensional — Análisis Exploratorio Integral"
subtitle: "Dataset: Comportamiento Crediticio y Riesgo Financiero (Semanas 1 a 3)"
author: "Estadística Multidimensional"
date: "`r format(Sys.Date(), '%d/%m/%Y')`"
output:
  html_document:
    toc: true
    toc_depth: 3
    number_sections: true
    df_print: paged
    theme: readable
    highlight: tango
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(
  echo = TRUE,
  comment = "#>",
  fig.width = 9,
  fig.height = 6,
  fig.align = "center",
  warning = FALSE,
  message = FALSE
)
# Ejecuta todos los chunks desde la raíz del proyecto, sin importar si RStudio
# hace knit desde analysis/ (opción "Document") o desde la raíz (opción "Project").
if (basename(getwd()) == "analysis") knitr::opts_knit$set(root.dir = "..")
```

# Contexto y Objetivos del Análisis

Este documento desarrolla la exploración multidimensional del conjunto de datos `data/bronze/synthetic_credit_card_customer_behavior_dataset.csv`. Integra de manera secuencial y exhaustiva los métodos, herramientas conceptuales y requerimientos desarrollados a lo largo de las **Semanas 1 a 3**:

* **Semana 1 (S01):** La matriz de datos $\mathbf{X}$, dimensiones ($n \times p$), clasificación de variables, medidas univariadas y bivariadas, y matrices de correlación y dispersión (`pairs`).
* **Semana 2 (S02):** Flujo de importación reproducible (`readLines`), auditoría de integridad, diagnóstico multivariante de datos faltantes (`complete.cases` vs `is.na`), verificación de duplicados e inconsistencias numéricas.
* **Semana 3 (S03):** Visualización en el espacio, impacto del número de clases en histogramas (`breaks` y regla de Sturges), análisis de dispersión y atípicos univariados con boxplots y la regla de Tukey ($1.5 \cdot RIC$), boxplots multivariados estandarizados (`scale`), y la demostración formal y gráfica del **"punto imposible"** (outlier multivariante no detectable univariadamente).

---

# Módulo 1: Inspección Previa e Importación Reproducible (S02)

## Mirar antes de leer (`readLines`)

Antes de cargar cualquier archivo en memoria, es una buena práctica de reproducibilidad inspeccionar sus primeras líneas en crudo para verificar delimitadores de columnas, separadores decimales y presencia de encabezados.

```{r inspeccion_previa}
ruta_credit <- "data/bronze/synthetic_credit_card_customer_behavior_dataset.csv"
lineas_muestra <- readLines(ruta_credit, n = 4)
lineas_muestra
```

**Diagnóstico preliminar:**
* **Separador de columnas:** Coma (`,`).
* **Separador decimal:** Punto (`.`).
* **Encabezado:** Presente en la primera fila con nombres descriptivos de variables.
* **Estrategia de lectura:** Corresponde utilizar `read.csv()` con `header = TRUE`, `sep = ","`, `dec = "."`, `stringsAsFactors = FALSE` y `na.strings` explícito para no depender de que R adivine qué texto representa un dato faltante.

## Importación explícita

```{r importacion}
credit <- read.csv(
  file = ruta_credit,
  header = TRUE,
  sep = ",",
  dec = ".",
  stringsAsFactors = FALSE,
  na.strings = c("", "NA", "N/A", "NULL")
)
```

> **Nota sobre `na.strings`:** se declaran solo los códigos textuales de faltante. No se incluyen centinelas numéricos como `-99` o `999` porque en este archivo `999` aparece como valor legítimo en `Reward_Points_Earned` / `Reward_Points_Redeemed`; convertirlo a `NA` sería corromper datos válidos. La búsqueda de centinelas numéricos se hace después, revisando los mínimos y máximos con `summary()`.

## Dimensiones de la matriz de datos ($\mathbf{X} \in \mathbb{R}^{n \times p}$)

```{r dimensiones}
dim_matriz <- dim(credit)
dim_matriz
```

La matriz de datos contiene:
* $n = `r nrow(credit)`$ observaciones (clientes simulados / filas).
* $p = `r ncol(credit)`$ variables medidas (columnas).

## Estructura y clasificación de variables (S01 & Everitt & Hothorn)

```{r estructura}
str(credit)
```

En la taxonomía del análisis multivariante (Everitt & Hothorn, Cap. 1), las $p = `r ncol(credit)`$ variables se clasifican en:

1. **Variables Cuantitativas / Numéricas (`r sum(sapply(credit, is.numeric))`):**
   * *Continuas de escala monetaria y gastos:* `Annual_Income`, `Monthly_Spending`, `Avg_Transaction_Value`, `Online_Shopping_Spending`, `Grocery_Spending`, `Fuel_Spending`, `Dining_Spending`, `Travel_Spending`, `Entertainment_Spending`, `Utility_Bill_Spending`, `Cash_Advance_Amount`, `Outstanding_Balance`, `Statement_Balance`, `Payment_Amount`, `Credit_Limit`.
   * *Continuas / Ratios e Índices:* `Payment_Ratio`, `Credit_Utilization`.
   * *Discretas / Conteos y Puntuaciones:* `Age`, `Card_Age_Months`, `Monthly_Transactions`, `EMI_Count`, `International_Transactions`, `Reward_Points_Earned`, `Reward_Points_Redeemed`, `Mobile_App_Login`, `Credit_Score`.
2. **Variables Cualitativas / Categóricas (`r sum(sapply(credit, is.character))`):**
   * *Nominales:* `Customer_ID` (identificador único), `Gender`, `Occupation`, `Card_Type`.

---

# Módulo 2: Auditoría y Diagnóstico de Calidad de la Matriz (S02)

## Diagnóstico multivariante de datos faltantes

Un concepto central de la estadística multivariante es que un dato faltante no es solo una celda vacía: al descartar una fila incompleta en un análisis conjunto, se pierde el vector completo $\mathbf{x}_i \in \mathbb{R}^p$.

```{r faltantes}
total_celdas <- prod(dim(credit))
celdas_na <- sum(is.na(credit))
pct_celdas_na <- (celdas_na / total_celdas) * 100

filas_completas <- sum(complete.cases(credit))
pct_filas_completas <- (filas_completas / nrow(credit)) * 100

# Conteo por columna
faltantes_por_col <- colSums(is.na(credit))
faltantes_por_col[faltantes_por_col > 0]
```

* **Total de celdas en la matriz:** `r format(total_celdas, big.mark = ",")`
* **Celdas vacías (`NA`):** `r celdas_na` (`r sprintf("%.2f%%", pct_celdas_na)`)
* **Filas completas (`complete.cases`):** `r format(filas_completas, big.mark = ",")` (`r sprintf("%.2f%%", pct_filas_completas)`)

> **Conclusión:** La matriz $\mathbf{X}$ está $100\%$ completa. No se presentan discrepancias entre estrategias de casos completos (`use = "complete.obs"`) y completitud por pares (`use = "pairwise.complete.obs"`).

## Verificación de duplicados e identificadores

```{r duplicados}
n_duplicados_filas <- sum(duplicated(credit))
n_duplicados_id <- sum(duplicated(credit$Customer_ID))
```

* **Filas exactamente duplicadas:** `r n_duplicados_filas`
* **Identificadores de cliente repetidos:** `r n_duplicados_id`

## Resumen univariado global y revisión de rangos (`summary`)

```{r resumen_general}
summary(credit)
```

Revisando mínimos y máximos no aparecen centinelas de error (`-99`, ingresos o edades negativas, ceros imposibles). El único valor que podría confundirse con un centinela es `999` en `Reward_Points_Earned` / `Reward_Points_Redeemed`, pero ahí es un conteo legítimo. Los rangos observados —edad $`r min(credit$Age)`$–$`r max(credit$Age)`$ años, `Payment_Ratio` $`r round(min(credit$Payment_Ratio), 2)`$–$`r round(max(credit$Payment_Ratio), 2)`$, `Credit_Utilization` $`r round(min(credit$Credit_Utilization), 2)`$–$`r round(max(credit$Credit_Utilization), 2)`$— son coherentes con las reglas de negocio crediticias.

## Tablas de frecuencia para variables categóricas

```{r tablas_frecuencia}
table(credit$Gender, useNA = "always")
table(credit$Card_Type, useNA = "always")
table(credit$Occupation, useNA = "always")
```

## Exportación de la capa *silver* (S02)

La matriz cruda (*bronze*) pasó las 8 verificaciones de importación sin necesidad de correcciones. Se guarda una copia trabajada en `data/silver/` —con `Customer_ID` movido a nombres de fila, ya que es un identificador y no una variable de análisis— para que los módulos siguientes y las semanas posteriores partan de un archivo estable y reproducible.

```{r exportacion_silver}
credit_silver <- credit
rownames(credit_silver) <- credit_silver$Customer_ID
credit_silver$Customer_ID <- NULL

dir.create("data/silver", showWarnings = FALSE, recursive = TRUE)
write.csv(credit_silver, "data/silver/credit_card_silver.csv", row.names = TRUE)

# Verificación: releer y comprobar estructura
check <- read.csv("data/silver/credit_card_silver.csv", row.names = 1)
c(filas = nrow(check), columnas = ncol(check),
  tipos_ok = all(sapply(check[sapply(credit_silver, is.numeric)], is.numeric)))
```

---

# Módulo 3: Análisis Univariado y la Decisión Gráfica del Histograma (S01 & S03)

## Media y desviación estándar de cada variable numérica (S01)

Antes de entrar a una variable concreta, se calcula en una sola instrucción el resumen de localización y dispersión de **todas** las variables cuantitativas.

```{r descriptivos_globales}
cols_num <- sapply(credit, is.numeric)
credit_num <- credit[, cols_num]

resumen_uni <- data.frame(
  Media    = sapply(credit_num, mean),
  Desv_Std = sapply(credit_num, sd),
  Min      = sapply(credit_num, min),
  Max      = sapply(credit_num, max)
)
round(resumen_uni, 2)
```

Las escalas son radicalmente dispares (desde ratios en $[0, 1]$ hasta ingresos del orden de $10^6$): esto anticipa la necesidad de estandarizar antes de comparar dispersión entre variables (Módulo 5) y antes de aplicar PCA en la unidad 6.

## Estadísticos descriptivos univariados

```{r descriptivos_univariados}
# Selección de la variable Credit_Score para estudio univariado
score <- credit$Credit_Score

media_cs <- mean(score)
sd_cs <- sd(score)
mediana_cs <- median(score)
min_cs <- min(score)
max_cs <- max(score)

c(Media = media_cs, Desv_Std = sd_cs, Mediana = mediana_cs, Min = min_cs, Max = max_cs)
```

## El histograma y el efecto del número de cortes (`breaks`) (S03 - Sesión 4)

El histograma es una estimación no paramétrica de la función de densidad subyacente. La elección de la cantidad de intervalos (`breaks` o ancho de clase $h$) altera profundamente la percepción de la distribución:

```{r histogramas_comparativos, fig.height=4, fig.width=10}
k_sturges <- nclass.Sturges(score)  # número de clases que propone la regla de Sturges

par(mfrow = c(1, 3))

# Histograma con muy pocos cortes (sobre-suavizado / underfitting)
hist(score, breaks = 5, col = "lightblue", border = "black",
     main = "breaks = 5 (Sobre-suavizado)",
     xlab = "Credit Score", ylab = "Frecuencia")

# Histograma con el número de clases de Sturges
hist(score, breaks = k_sturges, col = "lightgreen", border = "black",
     main = paste0("breaks = ", k_sturges, " (Sturges)"),
     xlab = "Credit Score", ylab = "Frecuencia")

# Histograma con demasiados cortes (ruidoso / overfitting)
hist(score, breaks = 60, col = "salmon", border = "black",
     main = "breaks = 60 (Ruidoso)",
     xlab = "Credit Score", ylab = "Frecuencia")

par(mfrow = c(1, 1))
```

## Regla de Sturges

La regla de Sturges propone el número óptimo de intervalos para muestras bajo supuestos de normalidad:
$$k = 1 + \log_2(n) = 1 + 3.322 \cdot \log_{10}(n)$$

```{r regla_sturges}
n_obs <- length(score)
k_sturges <- nclass.Sturges(score)
k_teorico <- 1 + log2(n_obs)

c(n = n_obs, k_Sturges = k_sturges, k_Formula = round(k_teorico, 2))
```

**Evaluación y decisión:**
Para $n = `r format(n_obs, big.mark = ",")`$, Sturges sugiere $k = `r k_sturges`$ intervalos ($1 + \log_2(n) \approx `r round(k_teorico, 1)`$). El histograma central, construido con esos `r k_sturges` cortes, es el que se declararía en un informe: captura la forma unimodal aproximadamente simétrica de los puntajes crediticios sin introducir los picos artificiales que produce `breaks = 60` por variabilidad muestral fina, ni el sobre-suavizado de `breaks = 5`.

---

# Módulo 4: Caja y Bigotes (Boxplots) y Detección Univariada de Atípicos (S03)

## Los cinco números de Tukey (`fivenum`)

Analizamos el gasto mensual (`Monthly_Spending`):

```{r cinco_numeros}
gasto <- credit$Monthly_Spending
f_gasto <- fivenum(gasto)
names(f_gasto) <- c("Mínimo", "Q1", "Mediana", "Q3", "Máximo")
f_gasto
```

## Rango Intercuartílico ($RIC$) y Límites de Tukey

El criterio de Tukey define las barreras para valores atípicos (*outliers*) como:
$$\text{Límite Inferior} = Q_1 - 1.5 \cdot RIC$$
$$\text{Límite Superior} = Q_3 + 1.5 \cdot RIC$$

```{r limites_tukey}
q1_gasto <- f_gasto["Q1"]
q3_gasto <- f_gasto["Q3"]
ric_gasto <- q3_gasto - q1_gasto

lim_inf_gasto <- q1_gasto - 1.5 * ric_gasto
lim_sup_gasto <- q3_gasto + 1.5 * ric_gasto

c(Q1 = q1_gasto, Q3 = q3_gasto, RIC = ric_gasto, 
  Lim_Inferior = lim_inf_gasto, Lim_Superior = lim_sup_gasto)
```

## Conteo de atípicos univariados

```{r conteo_atipicos}
atipicos_gasto <- gasto[gasto < lim_inf_gasto | gasto > lim_sup_gasto]
n_atipicos_gasto <- length(atipicos_gasto)
pct_atipicos_gasto <- (n_atipicos_gasto / length(gasto)) * 100

c(N_Atipicos = n_atipicos_gasto, Porcentaje = round(pct_atipicos_gasto, 3))
```

```{r boxplot_univariado, fig.height=4, fig.width=6}
boxplot(gasto, horizontal = TRUE, col = "skyblue",
        main = "Diagrama de Caja: Gasto Mensual (Monthly_Spending)",
        xlab = "Gasto Mensual ($)")
abline(v = c(lim_inf_gasto, lim_sup_gasto), col = "red", lty = 2, lwd = 2)
```

## ¿Es razonable marcar esa cantidad de atípicos? (pregunta clave de S03)

```{r diagnostico_asimetria}
razon_media_mediana <- mean(gasto) / median(gasto)   # > 1 indica cola derecha
razon_max_mediana   <- max(gasto) / median(gasto)

# ¿Cuántos atípicos quedan si primero se aplica log()?
gasto_log  <- log(gasto)
f_log      <- fivenum(gasto_log)
ric_log    <- f_log[4] - f_log[2]
lim_log    <- c(f_log[2] - 1.5 * ric_log, f_log[4] + 1.5 * ric_log)
n_atip_log <- sum(gasto_log < lim_log[1] | gasto_log > lim_log[2])

c(Razon_Media_Mediana = round(razon_media_mediana, 2),
  Razon_Max_Mediana   = round(razon_max_mediana, 1),
  Atipicos_escala_original = n_atipicos_gasto,
  Atipicos_tras_log        = n_atip_log)
```

**Reflexión crítica:**
La regla de Tukey marca `r format(n_atipicos_gasto, big.mark = ",")` observaciones (`r sprintf("%.2f%%", pct_atipicos_gasto)` del total) como atípicas por el lado superior. **No es un porcentaje despreciable**, y no se debe a errores de medición: `Monthly_Spending` tiene una **asimetría positiva marcada** —la media es `r round(razon_media_mediana, 2)` veces la mediana y el máximo llega a `r round(razon_max_mediana, 1)` veces la mediana—. Con una cola derecha larga, el criterio $Q_3 + 1.5 \cdot RIC$ deja fuera a toda la parte alta de la distribución aunque esos clientes sean perfectamente plausibles (gastadores altos, no errores). Es el mismo fenómeno que ilustra el conjunto 3 de Anscombe a la inversa: la regla es correcta, pero el supuesto de simetría que la hace interpretable no se cumple. Al aplicar `log()` antes de calcular los límites, el número de atípicos baja de `r format(n_atipicos_gasto, big.mark = ",")` a `r format(n_atip_log, big.mark = ",")`: la transformación reconstruye la simetría y devuelve sentido a la regla.

---

# Módulo 5: Comparación Multivariada Estandarizada mediante Boxplots (S03)

Cuando tenemos múltiples variables cuantitativas en diferentes unidades (pesos, meses, transacciones, ratios), no es posible graficarlas juntas en su escala original. Se reutiliza la submatriz numérica `credit_num` construida en el Módulo 3.

## Boxplots sin estandarizar vs. estandarizados (`scale`)

```{r comparativa_boxplots, fig.height=6, fig.width=11}
# Seleccionamos un subconjunto representativo de variables para visualización clara
vars_box <- c("Age", "Annual_Income", "Credit_Limit", "Card_Age_Months", 
              "Monthly_Spending", "Avg_Transaction_Value", "Outstanding_Balance",
              "Payment_Ratio", "Credit_Utilization", "Credit_Score")

par(mfrow = c(1, 2))

# 1. Sin estandarizar (las variables en millones dominan y ocultan a los ratios)
boxplot(credit_num[, vars_box], las = 2, col = "lightgray",
        main = "Sin Estandarizar (Escalas dispares)",
        ylab = "Valor Original")

# 2. Estandarizado (Z-scores: media 0, varianza 1)
credit_scale <- scale(credit_num[, vars_box])
boxplot(credit_scale, las = 2, col = "thistle",
        main = "Estandarizado con scale()",
        ylab = "Desviaciones Estándar (Z)")
abline(h = 0, col = "blue", lty = 3)
abline(h = c(-3, 3), col = "red", lty = 2)

par(mfrow = c(1, 1))
```

## Diagnóstico de colas máximas ($Z$-score máximo)

Identificamos a cuántas desviaciones estándar de su media se encuentra el valor máximo de cada variable:

```{r z_maximos}
z_max <- apply(credit_scale, 2, max)
round(sort(z_max, decreasing = TRUE), 2)
```

> **Lección de la Semana 3:** La estandarización `scale()` nivela el terreno de juego, permitiendo comparar simultáneamente la dispersión y la longitud relativa de las colas entre variables con órdenes de magnitud dispares ($10^6$ vs $10^{-1}$).

---

# Módulo 6: Análisis Bivariado, Correlación y el "Punto Imposible" (S01 & S03)

## Correlación bivariada y dispersión

Analizamos la relación entre el Ratio de Pago (`Payment_Ratio`) y el Puntaje Crediticio (`Credit_Score`):

```{r bivariado_intro}
r_pr_cs <- cor(credit$Payment_Ratio, credit$Credit_Score)
r_pr_cs
```

Existe una fuerte correlación lineal positiva ($r = `r round(r_pr_cs, 3)`$): los clientes que pagan una proporción mayor de su deuda exhiben sistemáticamente mejores puntajes de crédito.

## Construcción y Demostración del "Punto Imposible" (S03 - Sesión 4)

Uno de los conceptos fundamentales que distingue la **Estadística Multivariante** de la univariada es que **un individuo puede ser perfectamente normal en cada variable por separado y, sin embargo, ser un valor atípico imposible en el espacio conjunto**.

### 1. Verificación de los límites univariados de Tukey

```{r limites_punto_imposible}
f_pr <- fivenum(credit$Payment_Ratio)
f_cs <- fivenum(credit$Credit_Score)

iqr_pr <- f_pr[4] - f_pr[2]
iqr_cs <- f_cs[4] - f_cs[2]

lim_pr <- c(f_pr[2] - 1.5 * iqr_pr, f_pr[4] + 1.5 * iqr_pr)
lim_cs <- c(f_cs[2] - 1.5 * iqr_cs, f_cs[4] + 1.5 * iqr_cs)

c("Límites Payment_Ratio (Inf, Sup)" = lim_pr,
  "Límites Credit_Score (Inf, Sup)" = lim_cs)
```

### 2. Creación del cliente hipotético contradictorio

Diseñamos un cliente con:
* `Payment_Ratio = 0.95` (Paga casi el $100\%$ de su saldo).
* `Credit_Score = 460` (Puntaje crediticio extremadamente bajo / de alto riesgo).

```{r verificacion_punto}
punto_pr <- 0.95
punto_cs <- 460

# Verificación 1: ¿Está dentro de los límites de Tukey univariados?
dentro_pr <- punto_pr >= lim_pr[1] & punto_pr <= lim_pr[2]
dentro_cs <- punto_cs >= lim_cs[1] & punto_cs <= lim_cs[2]

data.frame(
  Variable = c("Payment_Ratio", "Credit_Score"),
  Valor = c(punto_pr, punto_cs),
  Lim_Inferior = c(lim_pr[1], lim_cs[1]),
  Lim_Superior = c(lim_pr[2], lim_cs[2]),
  Es_Normal_Univariado = c(dentro_pr, dentro_cs)
)
```

Ambos valores están estrictamente dentro de los bigotes univariados. Ningún filtro univariado lo marcaría como anómalo.

### 2.b Evidencia empírica: la vecindad conjunta está vacía

Comprobamos, con los datos, qué puntajes acompañan realmente a un ratio de pago tan alto:

```{r vecindad_punto}
clientes_pr_alto <- credit[credit$Payment_Ratio >= punto_pr, ]
n_pr_alto  <- nrow(clientes_pr_alto)
cs_min_obs <- min(clientes_pr_alto$Credit_Score)
cs_q01_obs <- quantile(clientes_pr_alto$Credit_Score, 0.01)

c(N_clientes_PR_mayor_igual_0.95 = n_pr_alto,
  Credit_Score_minimo_observado  = cs_min_obs,
  Percentil_1_Credit_Score       = round(cs_q01_obs, 0),
  Distancia_del_punto_al_minimo  = cs_min_obs - punto_cs)
```

### 3. Visualización bivariada: Demostración del "Punto Rojo"

```{r grafico_punto_imposible, fig.height=6, fig.width=8}
plot(credit$Payment_Ratio, credit$Credit_Score,
     pch = 16, col = rgb(0.2, 0.4, 0.8, 0.15),
     xlab = "Payment Ratio (Proporción de Pago)",
     ylab = "Credit Score (Puntaje Crediticio)",
     main = "El Punto Imposible: Normal Univariado vs. Atípico Multivariado")

# Dibujamos el punto en rojo
points(punto_pr, punto_cs, col = "red", pch = 19, cex = 2.2)
points(punto_pr, punto_cs, col = "black", pch = 1, cex = 2.2, lwd = 2)
text(punto_pr - 0.03, punto_cs - 20, labels = "Cliente Imposible\n(0.95, 460)", col = "red", font = 2, pos = 2)

# Cuadrantes de referencia
abline(v = mean(credit$Payment_Ratio), col = "gray50", lty = 2)
abline(h = mean(credit$Credit_Score), col = "gray50", lty = 2)
```

**Explicación conceptual:**
La combinación es contradictoria porque viola la covarianza del sistema. En esta población hay `r format(n_pr_alto, big.mark = ",")` clientes con `Payment_Ratio` $\geq 0.95$, y **ninguno baja de `r cs_min_obs` puntos** de `Credit_Score` (percentil 1 en `r round(cs_q01_obs, 0)`). El cliente hipotético, con puntaje `r punto_cs`, queda `r cs_min_obs - punto_cs` puntos por debajo del mínimo observado en toda esa franja: cae en una región del plano con densidad nula pese a ser plausible en cada eje por separado. Esto demuestra que **la normalidad marginal no garantiza la normalidad conjunta**.

---

# Módulo 7: Estructura Multivariada Conjunta (S01 & S03)

## Matriz de correlaciones completa ($\mathbf{R}$) (S01)

Primero, la matriz de correlación de **todas** las variables numéricas de análisis (se excluye `Customer_ID`, que es un identificador):

```{r matriz_correlacion_completa}
mat_cor_full <- cor(credit_num, use = "complete.obs")
round(mat_cor_full, 2)
```

### Par más y menos correlacionado (S01)

```{r par_extremos_correlacion}
m <- mat_cor_full
m[lower.tri(m, diag = TRUE)] <- NA          # solo el triángulo superior, sin la diagonal

idx_max <- which(abs(m) == max(abs(m), na.rm = TRUE), arr.ind = TRUE)[1, ]
idx_min <- which(abs(m) == min(abs(m), na.rm = TRUE), arr.ind = TRUE)[1, ]

data.frame(
  Relacion = c("Más correlacionadas (|r| máx)", "Menos correlacionadas (|r| mín)"),
  Var_1 = c(rownames(m)[idx_max[1]], rownames(m)[idx_min[1]]),
  Var_2 = c(colnames(m)[idx_max[2]], colnames(m)[idx_min[2]]),
  r     = round(c(m[idx_max[1], idx_max[2]], m[idx_min[1], idx_min[2]]), 4)
)
```

## Submatriz de variables estratégicas de negocio

```{r matriz_correlacion}
vars_interes <- c("Annual_Income", "Credit_Limit", "Monthly_Spending",
                  "Outstanding_Balance", "Statement_Balance", "Payment_Amount",
                  "Payment_Ratio", "Credit_Utilization", "Reward_Points_Earned", "Credit_Score")

mat_cor <- cor(credit_num[, vars_interes], use = "complete.obs")
round(mat_cor, 3)
```

### Síntesis de correlaciones observadas

```{r sintesis_correlacion}
pares <- which(upper.tri(mat_cor), arr.ind = TRUE)
tabla_pares <- data.frame(
  Var_1 = rownames(mat_cor)[pares[, 1]],
  Var_2 = colnames(mat_cor)[pares[, 2]],
  r     = round(mat_cor[upper.tri(mat_cor)], 3)
)
# Colinealidades más fuertes
head(tabla_pares[order(-abs(tabla_pares$r)), ], 5)
```

1. **Colinealidad estructural alta:** los pares con mayor $|r|$ de la tabla anterior (saldos entre sí, gasto con puntos de recompensa, cupo con ingreso) informan casi lo mismo: la matriz tiene dimensionalidad efectiva menor que sus `r ncol(credit_num)` columnas.
2. **Determinantes del riesgo:** `Payment_Ratio` con `Credit_Score` ($r = `r round(mat_cor["Payment_Ratio", "Credit_Score"], 3)`$, positiva) y `Credit_Utilization` con `Credit_Score` ($r = `r round(mat_cor["Credit_Utilization", "Credit_Score"], 3)`$, negativa).

## Matriz de dispersión multivariada (`pairs`)

Visualizamos las relaciones bivariadas simultáneas:

```{r pairs_plot, fig.height=8, fig.width=9}
vars_pairs <- c("Annual_Income", "Monthly_Spending", "Payment_Ratio", 
                "Credit_Utilization", "Credit_Score")

pairs(credit_num[, vars_pairs],
      pch = 16,
      col = rgb(0.1, 0.4, 0.7, 0.15),
      main = "Matriz de Dispersion (pairs) - Variables Clave")
```

## Reflexión teórica: la paradoja de las proyecciones bivariadas

```{r conteo_proyecciones}
p_num <- ncol(credit_num)
c(p_numericas = p_num, proyecciones_2D = choose(p_num, 2))
```

Con las `r ncol(credit_num)` variables numéricas de este conjunto existen $\binom{`r ncol(credit_num)`}{2} = `r choose(ncol(credit_num), 2)`$ diagramas de dispersión bivariados posibles (en el caso del vino visto en clase, con $p = 11$, eran $55$).

> **Pregunta fundamental:** Si examinamos todos los diagramas de dispersión 2D y ninguno muestra un punto fuera de las nubes, **¿podemos concluir que no hay valores atípicos en el conjunto?**
>
> **Respuesta metodológica:** **No.** De la misma manera que un punto puede ser normal en cada dimensión 1D pero imposible en 2D (el punto rojo), un individuo puede proyectarse dentro de las elipses de todas las parejas 2D y aun así ser un valor atípico extremo en un subespacio de dimensión $\geq 3$ (o a lo largo de una combinación lineal de variables). Para detectar estos casos se requieren métricas de distancia multivariante (como la Distancia de Mahalanobis $D^2$) y transformaciones factoriales (como PCA).

---

# Conclusiones del Análisis Multidimensional (Semanas 1 a 3)

1. **Conclusión Univariada (S01/S03):** Conviven dos tipos de forma. `Credit_Score`, `Payment_Ratio` y `Age` son aproximadamente simétricas y acotadas, y para ellas la regla de Sturges ($k = `r k_sturges`$ para $n = `r format(n_obs, big.mark = ",")`$) produce un histograma que refleja bien la distribución. En cambio las variables monetarias (`Monthly_Spending`, `Annual_Income`, balances) tienen **asimetría positiva fuerte**: la regla de Tukey marca como atípico un `r sprintf("%.1f%%", pct_atipicos_gasto)` de `Monthly_Spending` no por errores, sino porque el supuesto de simetría no se cumple; una transformación `log()` previa reduce esos atípicos a una fracción.
2. **Conclusión Bivariada (S01/S03):** El comportamiento de pago es el principal motor del puntaje de riesgo ($r = `r round(mat_cor["Payment_Ratio", "Credit_Score"], 3)`$), mientras que la utilización del cupo actúa como factor depresor ($r = `r round(mat_cor["Credit_Utilization", "Credit_Score"], 3)`$). La demostración del punto imposible muestra cómo un cliente con parámetros univariados plausibles (`0.95`, `460`) cae fuera de toda la nube conjunta —a `r cs_min_obs - punto_cs` puntos del mínimo real observado para ese ratio de pago— al romper la covarianza entre pago y riesgo.
3. **Conclusión Multivariada (S01/S02/S03):** La matriz $\mathbf{X}$ está $100\%$ completa y sin duplicados, pero posee alta interdependencia lineal: varios pares superan $|r| > 0.95$ (saldos, gasto–recompensas, cupo–ingreso), de modo que su dimensionalidad efectiva es bastante menor que sus `r ncol(credit_num)` columnas numéricas. La inspección estandarizada (`scale`) y la matriz `pairs` confirman que el conjunto está preparado para técnicas de reducción de dimensionalidad (PCA) en la unidad 6.

